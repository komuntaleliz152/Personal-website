services:
  - type: web
    name: elizabeth-portfolio
    runtime: static
    buildCommand: echo "No build required"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
